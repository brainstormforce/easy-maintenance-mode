function show_title(x) {
	// Show current range on the field having 'slider_title' id
	document.getElementById("slider_title").innerHTML=x+"px";
}

function show_description(y) {
	// Show current range on the field having 'slider_desc' id
	document.getElementById("slider_desc").innerHTML=y+"px";
}

function show_countdown(z){
	// Show current range on the field having 'slider_countdown' id	
	document.getElementById("slider_countdown").innerHTML=z+"px";
}

function show_container(z){
	// Show current range on the field having 'slider_container' id
	document.getElementById("slider_container").innerHTML=z;
}
